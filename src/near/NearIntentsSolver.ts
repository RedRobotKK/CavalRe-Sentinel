/**
 * MOVED: this scaffold has been superseded by the tested implementation in
 * `packages/near-solver` (@cavalre/near-solver).
 *
 * See packages/near-solver/README.md and docs/NEAR_INTEGRATION.md.
 * This file is retained only to avoid a dangling reference and may be deleted:
 *   git rm -r src/near
 */
export {};
