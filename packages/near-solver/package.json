{
  "name": "@cavalre/near-solver",
  "version": "0.1.0",
  "description": "NEAR Intents solver: FloatLib pricing, ledger-backed inventory, NEP-413 signing, fail-closed risk",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "license": "MIT",
  "dependencies": {
    "@cavalre/floatlib-ts": "file:../floatlib-ts",
    "@cavalre/ledger-ts": "file:../ledger-ts"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.1.0",
    "@types/node": "^20.10.0"
  }
}
